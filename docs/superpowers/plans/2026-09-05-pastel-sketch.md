# 日式小清新彩色素描

用户要求素描风格必须有配色，采用日式小清新配色。本次是现有
`anime-sketch` 设计的配色修订，保留线稿、纸纹、构图、官方卡面和透明角色。

配色采用奶油白、薄荷绿、雾蓝、蜜桃粉、浅杏黄；在道具和环境上形成可辨认的
彩铅色块，深青绿标题保证阅读对比。背景生成负责实际颜色，合成不使用全图
染色来代替彩色素材。旧背景保留，新背景为 `tavern_bg_pastel.png`。

- [x] 将默认提示词从纸白石墨为主改成清晰可见的日式彩铅淡彩；验证两个后端。
- [x] 生成并保存彩色背景、提示词；调整封面重点文字为深青绿。
- [x] 检查封面、移动端缩图和短片头；同步画风说明和使用示例。
- [x] 运行测试与独立审阅；通过后按原收尾请求提交、同步并保持工作区干净。

验收：两个后端的彩色提示词检查先失败后通过；遮罩合成保色测试先失败后通过。全量 795 项测试通过，独立审阅 60 项相关测试通过，`git diff --check` 通过。已检查 1280×720 封面、320×180 缩图和 3 秒片头。新背景由内置 ImageGen 生成，原背景与透明角色保留。
